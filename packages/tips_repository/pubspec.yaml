name: tips_repository
description: A starting point for Dart libraries or applications.
version: 1.0.0
# homepage: https://www.example.com

publish_to: 'none'

environment:
  sdk: '>=2.17.3 <3.0.0'

dependencies:
  dio: ^4.0.6
  tips_and_tricks_api:
    path: ../tips_and_tricks_api
  freezed_annotation: ^2.0.3
  json_annotation: ^4.5.0    

dev_dependencies:
  lints: ^2.0.0
  test: ^1.16.0
  freezed: ^2.0.3+1  
  json_serializable: ^6.2.0
  build_runner: ^2.1.10  
